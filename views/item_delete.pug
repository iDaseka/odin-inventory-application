extends layout 

block content
    h1 #{title}: #{item.name}

    p Do you want to delete this #[strong Item]?
    form(method='POST')
        div.form-group
            input#id.form-control(type='hidden' name='id' value=item._id)
        button.btn.btn-primary(type='submit') Delete